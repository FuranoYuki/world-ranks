import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  justify-content: center;
  flex-shrink: 0;
  width: 100%;

  margin-bottom: 40px;

  &:hover {
    cursor: pointer;
  }
`;
